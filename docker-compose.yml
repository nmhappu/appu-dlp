services:
  dl-plex:
    build: .
    container_name: dl-plex-downloader
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - NODE_ENV=production
      - PORT=${PORT:-3000}
