import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nmhappu.appudlp',
  appName: 'appu-dlp',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
